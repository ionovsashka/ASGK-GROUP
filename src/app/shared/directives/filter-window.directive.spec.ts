import { FilterWindowDirective } from './filter-window.directive';

describe('FilterWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterWindowDirective();
    expect(directive).toBeTruthy();
  });
});
