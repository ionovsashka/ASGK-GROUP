import { ModalWindowDirective } from './modal-window.directive';

describe('ModalWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalWindowDirective();
    expect(directive).toBeTruthy();
  });
});
