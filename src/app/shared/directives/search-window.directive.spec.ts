import { SearchWindowDirective } from './search-window.directive';

describe('SearchWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchWindowDirective();
    expect(directive).toBeTruthy();
  });
});
